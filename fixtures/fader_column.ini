# fader_column — fader-column face fixture for the physical view (task 1.3).
# Bare [p8s8] + [m4] synthesize the full faceplates: P8S8 Faderbank = 8
# sliders (P1.1..P1.8, F-family cells addressed via P registers) + 8 slider
# LEDs (L1.1..L1.8) + 8 switches (S1.1..S1.8); M4 Motorfader = 4 motor faders
# (P2.1..P2.4) + 4 touch buttons (B2.1..B2.4) + 4 LEDs (L2.1..L2.4). The two
# controller chains sit side by side in one rack row, so the vertical
# bottom-up `▮`/`▯` track renders on both faceplates at different cell
# heights. Tests drive the first fader of each panel (P1.1, P2.1) to
# 0/50/100 % and assert the lit count mirrors round(value × cell height)
# across zoom presets 0.75/1.0/1.5/2.0 (0% all-dim track, 100% full lit).

[p8s8]

[m4]