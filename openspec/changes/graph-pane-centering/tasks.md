# Tasks: graph pane centering, fit, and deeper zoom-out

Reference: specs (`signal-flow-graph`, `keybinding` deltas) for behavior, design.md for how.

## 1. Camera helpers and zoom presets (src/app.rs)

- [ ] 1.1 Extend `GRAPH_ZOOM_PRESETS` to `[0.03125, 0.0625, 0.125, 0.25, 0.5, 0.75, 1.0, 1.5, 2.0]` (9 entries, `0.03125` is exactly 2× below the previous minimum), add a named `GRAPH_ZOOM_FIT_INDEX` constant for the `1.0` entry (now index 6) with a doc note, and replace the hardcoded `5` at both reset sites (src/app.rs:2643 `open_graph`, src/app.rs:3101) with the constant; verify the preset table test asserts 9 entries, min `0.03125`, fit index 6, and the existing zoom wrap tests still pass <!-- agent: rusty-engineer.build, depends_on: [], touches: [src/app.rs] -->
- [ ] 1.2 Add `App::center_graph_camera()`: pan the camera so the drawn bounds' center (dependency-filter aware, reusing the main.rs:217-225 positions selection) maps to the visible pane's center via `graph_canvas_px`; keep zoom; no-op (returns false, no status) when `graph_camera` or `graph_canvas_px` is `None`; verify an app.rs unit test: published canvas px + seeded camera, centering shifts `pan` so the bounds center maps to the canvas center, zoom unchanged, and a second test asserts the no-op before any render <!-- agent: rusty-engineer.build, depends_on: [1.1], touches: [src/app.rs] -->
- [ ] 1.3 Add `App::fit_graph_camera(viewport: (f32, f32))`: full refit of the dependency-filter-aware bounds against `viewport` via `fit_to_world` with the min-node-px, set `graph_zoom_preset` to `GRAPH_ZOOM_FIT_INDEX`, and publish `graph_canvas_px = Some(viewport)`; verify an app.rs unit test fits against a non-1280×800 viewport (e.g. 640×300) and asserts the preset equals the fit index and the canvas px is published <!-- agent: rusty-engineer.build, depends_on: [1.1], touches: [src/app.rs] -->

## 2. Canvas publication and viewport-aware first-frame fit (src/gui, src/main.rs)

- [ ] 2.1 In `paint_tiled` (src/gui/mod.rs:629) Graph-slot branch and the quad-path graph pane (src/gui/mod.rs:613), publish `app.graph_canvas_px = Some((rect.width(), rect.height()))` every frame, and seed the camera when `graph_camera.is_none()` with `fit_graph_camera((rect.width(), rect.height()))` before painting; verify a gui-module shape test drives the paint path and asserts `graph_canvas_px` equals the slot rect and the camera is seeded after the first frame <!-- agent: rusty-engineer.build, depends_on: [1.3], touches: [src/gui/mod.rs] -->
- [ ] 2.2 In `GraphWindow::paint` (src/gui/mod.rs:725), publish `graph_canvas_px` from the canvas size in points and seed when `None`; verify the window paint test asserts `graph_canvas_px` is set to the canvas size after a frame <!-- agent: rusty-engineer.build, depends_on: [1.3], touches: [src/gui/mod.rs] -->
- [ ] 2.3 Give `graph_window_fit_camera` (src/gui/graph.rs:250) a `viewport: (f32, f32)` parameter replacing the `WINDOW_FIT_VIEWPORT_W/H` constants, and update its three call sites: `main.rs` RedrawRequested passes the window's inner size in points (`inner_size / scale_factor`) as the first-frame fallback, the tile/quad paths pass the slot rect, `GraphWindow::paint` passes the canvas size; keep the dependency-filter-aware fit selection (main.rs:217-225); verify the existing `window_paint_ready_fits_scene_into_canvas` test (src/gui/graph.rs:2084) passes with an explicit viewport and `cargo test` stays green <!-- agent: rusty-engineer.build, depends_on: [1.3], touches: [src/gui/graph.rs, src/gui/mod.rs, src/main.rs] -->

## 3. Key routing (src/handler.rs, src/gui/mod.rs, src/help.rs)

- [ ] 3.1 In the graph-surface branch of `handle_event`: `Char('c')` calls `center_graph_camera()`, new `Char('C')` (Shift+c) calls `fit_graph_camera` with the published canvas px, and latency coloring moves to a new `c` arm in the g-prefix branch (src/handler.rs:547-617, beside `v/g/d/o/w/s`) calling `toggle_latency_coloring` and clearing the prefix; verify handler tests: bare `c` on an open graph centers (pan changes, zoom unchanged, latency coloring untouched), `Shift+c` refits (zoom resets to the fit index), `g` then `c` toggles latency with the existing status text, and `c` no longer flips `latency_coloring` <!-- agent: rusty-engineer.build, depends_on: [1.2, 1.3], touches: [src/handler.rs] -->
- [ ] 3.2 Add `CenterGraph` and `FitGraph` variants to `WindowGraphKey` (src/gui/mod.rs:78), map `Key::C` / Shift+C in the window-keys extraction (src/gui/mod.rs:779-781), and dispatch both in `handle_graph_window_frame` (src/handler.rs:2008) to the same `center_graph_camera`/`fit_graph_camera` calls; verify window-frame tests (pattern at src/handler.rs:5237-5312) drive `CenterGraph`/`FitGraph` and assert the camera pan/zoom changes <!-- agent: rusty-engineer.build, depends_on: [1.2, 1.3, 2.3], touches: [src/gui/mod.rs, src/handler.rs] -->
- [ ] 3.3 Update `src/help.rs`: graph key table row `c` becomes "center graph", add `Shift+c` "fit and center graph", and the g-prefix table gains `g c` "toggle latency coloring"; verify the help-content test asserts the new rows and that no "c = toggle latency coloring" row remains <!-- agent: rusty-engineer.build, depends_on: [1.2], touches: [src/help.rs] -->

## 4. Regression updates, docs, and full gate

- [ ] 4.1 Update the existing tests that hardcode preset indices and the latency key: `c_key_toggles_latency_coloring_on_graph_surface` (src/handler.rs:4936) becomes a `g c` chord test, and the preset assertions at src/handler.rs:2634/2649/2695 and any app.rs tests use the new list/fit index; verify `cargo test` passes <!-- agent: horst-engineer.build, depends_on: [3.1, 3.2], touches: [src/handler.rs, src/app.rs] -->
- [ ] 4.2 Update ARCHITECTURE.md and DESIGN.md where they describe the graph `c` key, the first-frame camera seed viewport, or the zoom preset list (search for `latency coloring`, `1280`, `GRAPH_ZOOM_PRESETS`, `graph_camera` in the docs) to match the new behavior; verify `grep -rn` shows no stale `c`-toggles-latency or 1280×800 fit claim remains in the docs <!-- agent: devops-engineer.build, depends_on: [], touches: [ARCHITECTURE.md, DESIGN.md] -->
- [ ] 4.3 Run the full verification gate: `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test`, `cargo build --release --locked`; verify all four exit 0 <!-- agent: devops-engineer.build, depends_on: [4.1, 4.2], touches: [] -->
- [ ] 4.4 Live-screen visual verification (per project rule: green tests are not evidence the visible symptom is gone): drive the built binary in a screen session at the target COLUMNS/LINES, load a patch, open the graph, press `c` and `Shift+c` and `g c`, and capture a hardcopy proving the graph centers in the visible pane (not the window) and zoom-out reaches below half the fitted scale; verify the hardcopy shows the graph centered in the pane at a zoom-out below the previous minimum <!-- agent: rusty-engineer.build, depends_on: [4.3], touches: [] -->