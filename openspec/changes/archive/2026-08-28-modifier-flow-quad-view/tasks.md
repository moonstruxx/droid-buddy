## Tasks

- [x] 1.1 Add circuit-output reverse map + `influence_subtree` BFS walk (structural hops, cycle-safe, deterministic) to `patch.rs` with HW->VAR derivation <!-- agent: dermannmitdermachine-engineer.build, depends_on: [], touches: [src/patch.rs] -->
- [x] 1.2 Add `InfluenceSubtree` model and `filtered_influence` induced-subgraph builder + highlight-set fields to `graph.rs` <!-- agent: api-engineer.build, depends_on: [1.1], touches: [src/graph.rs] -->
- [x] 2.1 Add highlight/dim + compact-filtered theme tokens and palettes (`graph_node_highlight`, `graph_edge_highlight`, `graph_edge_dim`, `graph_node_dim`) <!-- agent: layout-designer-engineer.build, depends_on: [], touches: [src/theme.rs] -->
- [x] 2.2 Add filtered-subgraph independent `solve()` invocation (compact convergence) for the FILTERED pane <!-- agent: api-engineer.build, depends_on: [1.2], touches: [src/layout.rs] -->
- [x] 3.1 Add quad-view state and influence recomputation to `App` (`showing_quad`, `quad_focus`, `active_modifier_var`, `influence`, `filtered_graph/positions`, `recompute_influence` on modifier selection) and `Esc`/`Tab`/`g g`/`g v` + drag handling for both graph panes; wire optional `InfluenceRecomputed` event <!-- agent: rusty-engineer.build, depends_on: [1.2, 2.1], touches: [src/app.rs, src/handler.rs, src/events.rs] -->
- [x] 3.2 Implement quad 4-pane concurrent layout in `ui.rs` (top `panels | source`, bottom `graph FULL | graph FILTERED`; highlight/dim in FULL, compact positions in FILTERED; responsive fallback below ~120 cols; feature-flagged `kitty-gfx` fallback to box-drawing) <!-- agent: layout-designer-engineer.build, depends_on: [3.1, 2.2], touches: [src/ui.rs] -->
- [x] 4.1 Unit + layout tests: BFS direct/indirect via `switch`, copy-chain, cycle-safe, leaf/any-input+output hop, HW->VAR derivation, filtered membership, highlight sets, filtered compact solve <!-- agent: horst-engineer.build, depends_on: [1.2, 2.2], touches: [src/patch.rs, src/graph.rs, src/layout.rs, fixtures/modifier_switch_passthrough.ini] -->
- [x] 4.2 Snapshot + gallery tests for quad view: 4-pane frames × themes (`classic`/`mono`/`terminal`) × widths × modifier-selected states (FULL highlight vs FILTERED compact), extended visual-validation matrix <!-- agent: horst-engineer.build, depends_on: [3.2], touches: [src/snapshots/**, evidence/gallery/**, fixtures/modifier_switch_passthrough.ini] -->
