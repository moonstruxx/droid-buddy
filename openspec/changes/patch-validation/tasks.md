# Tasks: patch-validation

- [ ] 1.1 Vendor `droid-lsp` as `ext/droid-lsp` submodule + `src/schema.rs` loader (`include_str!`, `loadSchema`, `JackTable`, prefix/count expansion, Levenshtein helper) — wire via `Cargo.toml` <!-- agent: dermannmitdermachine-engineer.build, tier: build, depends_on: [], touches: [ext/droid-lsp, src/schema.rs, Cargo.toml] -->
- [ ] 1.2 Extend `src/patch.rs` `IniSection` with per-entry `Span`s (`key_span`/`value_span`/`line`) and capture in `parse_ini_sections` — preserve `raw_lines`/`header_span` <!-- agent: rusty-engineer.build, tier: build, depends_on: [1.1], touches: [src/patch.rs] -->
- [ ] 2.1 Implement pure `src/validation.rs` `Severity`/`ValidationIssue` + `validate_patch` — port 9 `droid-lsp/diagnostics.ts` checks 1:1 with `Span` (header vs value), sorted output, RAM budget via `ramsize` <!-- agent: dermannmitdermachine-engineer.build, tier: build, depends_on: [1.2], touches: [src/validation.rs, src/schema.rs] -->
- [ ] 2.2 Integrate into `src/app.rs`/`src/events.rs` — `validation_issues`, `showing_validation`, `Event::ValidationCompleted`, `load_patch` fail-on-Error gating + `clear_validation` <!-- agent: rusty-engineer.build, tier: build, depends_on: [2.1], touches: [src/app.rs, src/events.rs] -->
- [ ] 3.1 Add modal error list in `src/ui.rs`/`src/theme.rs`/`src/handler.rs` — `render_validation_modal` (line:col + severity badge), theme tokens, `e`/`j/k`/`Enter`/`Esc` <!-- agent: layout-designer-engineer.build, tier: build, depends_on: [2.2], touches: [src/ui.rs, src/theme.rs, src/handler.rs] -->
- [ ] 3.2 Add `regression.rs` + `fixtures/validation/*.ini` matrix — one fixture per check (unknown circuit/param, duplicate param, invalid jack, missing required, undefined/duplicate/unused cable, RAM) × `classic`/`terminal`/`mono` snapshots + `validation.rs` unit tests <!-- agent: horst-engineer.build, tier: build, depends_on: [2.1], touches: [src/validation.rs#tests, src/schema.rs#tests, src/regression.rs, fixtures/validation/*] -->
